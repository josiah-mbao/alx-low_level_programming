#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: the inegert to check
 * Return: the abs value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
